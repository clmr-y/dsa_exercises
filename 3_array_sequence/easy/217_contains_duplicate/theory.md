## Bruteforce
$ O(n^2) $ - by looping twice

$ O(1) $ - space

## Sorting
$ O(n\log n) $ - by sorting before hand
$ O(1) $ - space

We use two pointers to check if next-values are similar
[1,1,2,3,4]


## HashSet
$ O(n) $ - by once over hash set

$ O(b) $ - space
